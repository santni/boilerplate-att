import { StyleSheet } from "react-native";

const styles = StyleSheet.create({ 
        text: {
            fontSize: 20,
        },
        title: {
            fontSize: 24,
            fontWeight: "bold",
            marginBottom: 16,
        }
    });

    export default styles;
